<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" sizes="16x16" href="/css/fav.png" />

    <title>Login</title>
    <link rel="stylesheet" href="/styles/style.css" />
  </head>
  <body class="login-body">
    <h2>Login</h2>
    <% if (error) { %>
    <p><%= error %></p>
    <% } %>
    <form action="/login" method="post">
      <input
        type="text"
        id="username"
        name="username"
        placeholder="Username"
        required
      />
      <input
        type="password"
        id="password"
        name="password"
        placeholder="Password"
        required
      />
      <button class="button-4" type="submit">Login</button>
    </form>
    <div class="register-link">
      Don't have an account? <a href="/register">Create a new account</a>
    </div>
  </body>
</html>
