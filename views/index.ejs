<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" sizes="16x16" href="/css/fav.png">
    <title>Ethentix - Shantosh Seelam</title>
    <link rel="stylesheet" href="styles/style.css" />
    <style>
        body {
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            overflow-x: hidden;
            background-color: #121212;
        }

        header {
            background-color: #2c3e50;
            margin-top: 10%;
            color: #ffffff;
            text-align: center;
            padding: 20px;
        }

        section {
            max-width: 600px;
            margin: 20px auto;
            text-align: center;
        }

        h1 {
            margin: 0;
            font-size: 2em;
            color: #333333;
        }

        p {
            font-size: 1.2em;
            line-height: 1.5;
            color: #555555;
            margin-bottom: 20px;
        }

        a {
            color: #3498db;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        footer {
            background-color: #555555;
            color: #ffffff;
            text-align: center;
            padding: 10px;
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        .news-scroll {
            padding: 10px;
            overflow: hidden;
            white-space: nowrap;
            animation: marquee 20s linear infinite;
        }

        @keyframes marquee {
            0% {
                transform: translateX(100%);
            }
            100% {
                transform: translateX(-100%);
            }
        }

        /* Responsive Styles */
        @media screen and (max-width: 600px) {
            header {
                margin-top: 20%;
            }

            section {
                max-width: 90%;
            }
        }

        @media screen and (max-width: 768px) {
            section {
                max-width: 80%;
            }
        }
    </style>
</head>
<body class="font-sans">
    <div class="news-scroll">
        <p style="color: red;">Data will be erased after 30 days, and accounts will be removed.</p>
    </div>

    <header>
        <h1 style="color: whitesmoke;">Welcome to the URL Shortener</h1>
    </header>

    <section>
        <p>Shorten your URLs and manage them with ease.</p>
        <div>
            <a href="/auth/login">Login</a>
            <span>or</span>
            <a href="/auth/register">Register</a>
        </div>
    </section>

    <footer>&copy; 2023 Shantosh Seelam. All rights reserved.</footer>
</body>
</html>
